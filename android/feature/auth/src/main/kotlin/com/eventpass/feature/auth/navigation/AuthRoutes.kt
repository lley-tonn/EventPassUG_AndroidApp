package com.eventpass.feature.auth.navigation

/** Route constants for the auth flow. Register these from :app's NavHost. */
object AuthRoutes {
    const val CHOICE = "auth/choice"
    const val LOGIN = "auth/login"
    const val SIGN_UP = "auth/signup"
}
